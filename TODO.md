- Switch metadata locale for spanish to es-ES and re-enable the Play Store metadata check? Or switch en-US to en and leave it off? I am currently inconsistent, using an irrelevant region code for one locale and not for the other.

- Should we be using TextFieldState and the associated rememberTextFieldState() function instead of working with TextFieldValue?
