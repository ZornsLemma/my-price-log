package app.zornslemma.mypricelog.debug

// All of these should be set to false for a tagged release unless there's some specific reason to
// leave them enabled.
object DebugFlags {
    const val USE_STRICT_MODE = false
    const val LOG_SQL = false
    const val ALLOW_ROTATION_ON_PHONE = false
}
